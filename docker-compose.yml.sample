version: '3'

services:
  quepasa:
    image: quepasa
    ports:
      - "3000:3000"
    environment:
      - APP_ENV=production
      - SIGNING_SECRET=changeme
      - DB_CONNECTION=user=siggy dbname=siggy_dev
